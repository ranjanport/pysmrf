[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pysmrf"
version = "2.0.0"
description = "High-performance parallel Simple Morphological Filter (SMRF) for LiDAR ground classification and DEM generation"
readme = "README.md"
license = "MIT"
authors = [
    { name = "PySMRF Contributors" },
    { name = "Thomas J. Pingel", email = "thomas.pingel@gmail.com" }
]
maintainers = [
    { name = "PySMRF Contributors" }
]
keywords = ["lidar", "point-cloud", "gis", "dem", "dtm", "morphological-filter", "ground-classification", "parallel", "remote-sensing"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "Topic :: Scientific/Engineering :: GIS",
    "Topic :: Scientific/Engineering :: Image Processing",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent"
]
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.22.0",
    "scipy>=1.8.0",
    "rasterio>=1.3.0",
    "pandas>=1.4.0",
    "laspy>=2.0.0",
    "click>=8.0.0"
]

[project.optional-dependencies]
laz = [
    "lazrs>=0.5.0"
]
viz = [
    "matplotlib>=3.5.0"
]
all = [
    "lazrs>=0.5.0",
    "matplotlib>=3.5.0",
    "scikit-image>=0.19.0"
]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0"
]

[project.urls]
Homepage = "https://github.com/thomaspingel/smrf"
Paper = "https://doi.org/10.1016/j.isprsjprs.2012.12.002"
Repository = "https://github.com/thomaspingel/smrf"

[project.scripts]
pysmrf = "pysmrf.cli:cli"

[tool.setuptools.packages.find]
include = ["pysmrf*"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
